1) Run uas_parameters
2) Run uas_trajectory
3) run ex64 simulink model

The trajectory is set by changing the waypoint (points it flies through) and corridors (are where the drone must stay).

Knots and corridors.times is a time when the droned approach the set values